### How to install
```shell

```
### Usage:
```shell
     elbowaicli \
     --epochs=200 \
     --lr=0.005 \
     --model="RandomForest" \
     --square=25 \
     --output=config.csv \
     --verbosity=2
     
python app.py \
    --epochs 200 \
    --lr 0.005 \
    --model "RandomForest" \
    --square 25 \
    --output config.csv \
    --verbosity 2
     
python app.py -e=200 -l=0.005 -m="RandomForest" -s=25 -o=config.csv -v=2 
python app.py -e 200 -l 0.005 -m "RandomForest" -s 25 -o config.csv -v 2
  
```